# Review for abc436_a

*(AI generation skipped or not configured)*

## Source Code
```
N=int(input())
S=input()

M=len(S)
P=N-M

print('o'*P + S)
```

To enable AI reviews, configure `ai_provider` in `config.json`.
